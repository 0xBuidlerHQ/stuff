import { Box } from "@/primitives/box";
import { Container } from "@/primitives/container";

const Page = () => {
	return (
		<Container>
			<Box className="text-8xl">CULTURE</Box>
		</Container>
	);
};

export default Page;
